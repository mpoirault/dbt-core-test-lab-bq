name: 'dbt_core_test'
version: '1.0.0'
config-version: 2
profile: 'dbt_core_test'

model-paths:      ["models"]
analysis-paths:   ["analyses"]
test-paths:       ["tests"]
seed-paths:       ["seeds"]
macro-paths:      ["macros"]
snapshot-paths:   ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

on-run-end:
  - "{{ log_bq_jobs(region='EU') }}"
    
query-comment: 
  job-label: true
  comment: "{{ query_comment(node) }}"
