---
models:
  - name: dim_date
    description: >
      Date dimension generated by
      dbt_date.get_date_dimension(). Column set comes from the package; only
      the key column is documented here.
    config:
      meta:
        criticality: low
        pii: false
    columns:
      - name: date_day
        description: Calendar day, the primary key.
        data_tests:
          - unique
          - not_null
