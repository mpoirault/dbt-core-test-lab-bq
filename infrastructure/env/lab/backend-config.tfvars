# One env, it is a lab. More envs get their own folder here,
# each with a backend-config and a vars file, and a terraform workspace.
bucket = "dbt-core-test-lab-bq-tfstate"
prefix = "terraform/state"
